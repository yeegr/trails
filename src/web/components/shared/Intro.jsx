'use strict'

import React, {
  PropTypes
} from 'react'

const Intro = (props) => {
  return (
    
  )
}

Intro.propTypes = {
  title: PropTypes.string.isRequired,
  excerpt: PropTypes.string,
  tags: PropTypes.array
}

export default Intro